/*
package be.fedasil.matchit.frontend.pages.template;

import be.fedasil.matchit.backend.model.Place;
import be.fedasil.matchit.frontend.menu.MainMenuItem;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;

*/
/**
 * Created by tmaes on 8/05/2015.
 *//*

public class NormalPagePresenterTest {

    @InjectMocks
    private NormalPagePresenter classToTest;

    @Mock
    private PageView viewMock;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testOnMenuSelectedNotExisting() {
        Mockito.when(viewMock.setCurrentMenuItem(null)).thenReturn(null);

//        Assert.assertNull(place);
    }
}
*/
